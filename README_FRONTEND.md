# Frontend Setup & Execution

Follow these step-by-step commands to set up and run the React/Vite frontend:

1. `cd frontend`
2. `npm install`
3. `npm run dev`
4. Ensure frontend `.env` sets: `VITE_API_BASE_URL=http://127.0.0.1:8001`
